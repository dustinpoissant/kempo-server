#!/usr/bin/env node
import http from"http";import router from"./router.js";import{closeAllSockets}from"./websocket/index.js";import getFlags from"./getFlags.js";const flags=getFlags(process.argv.slice(2),{port:3e3,logging:2,root:"./",config:".config.js"},{p:"port",l:"logging",r:"root",c:"config"});if("string"==typeof flags.logging)switch(flags.logging.toLowerCase()){case"silent":flags.logging=0;break;case"minimal":flags.logging=1;break;case"verbose":flags.logging=3;break;case"debug":flags.logging=4;break;default:flags.logging=2}const log=(message,level=2)=>{level<=flags.logging&&console.log(message)},handler=await router(flags,log),server=http.createServer(handler);server.on("upgrade",handler.upgrade);const shutdown=()=>{const closed=closeAllSockets();closed&&log(`Closed ${closed} WebSocket connection(s)`,2),server.close(()=>process.exit(0)),setTimeout(()=>process.exit(0),2e3).unref()};process.on("SIGINT",shutdown),process.on("SIGTERM",shutdown),server.listen(flags.port),log(`Server started at: http://localhost:${flags.port}`);
import{URL}from"url";export const DEFAULT_MAX_BODY_SIZE=524288e3;export const readRawBody=(req,maxBodySize=524288e3)=>void 0!==req._bufferedBody?Promise.resolve(req._bufferedBody):new Promise((resolve,reject)=>{const chunks=[];let size=0;req.on("data",chunk=>{if(size+=chunk.length,size>maxBodySize)return req.destroy(),void reject(new Error("Payload Too Large"));chunks.push(chunk)}),req.on("end",()=>{resolve(Buffer.concat(chunks))}),req.on("error",reject)});export const parseBody=(rawBody,contentType)=>{if(!rawBody||0===rawBody.length)return null;const ct=(contentType||"").toLowerCase();if(ct.includes("application/json"))try{return JSON.parse(rawBody.toString("utf8"))}catch{return null}return ct.includes("application/x-www-form-urlencoded")?Object.fromEntries(new URLSearchParams(rawBody.toString("utf8"))):rawBody.toString("utf8")};export function createRequestWrapper(request,params={}){const url=new URL(request.url,`http://${request.headers.host||"localhost"}`),query=Object.fromEntries(url.searchParams);return{...request,_originalRequest:request,method:request.method,url:request.url,headers:request.headers,params:params,query:query,path:url.pathname,cookies:(()=>{const cookieHeader=request.headers.cookie||request.headers.Cookie;return cookieHeader?cookieHeader.split(";").reduce((cookies,cookie)=>{const[name,...rest]=cookie.trim().split("=");return name&&(cookies[name]=rest.join("=")),cookies},{}):{}})(),body:null,_rawBody:"",async json(){return JSON.parse(this._rawBody.toString("utf8"))},async text(){return this._rawBody.toString("utf8")},async buffer(){return Buffer.from(this._rawBody)},get:headerName=>request.headers[headerName.toLowerCase()],is(type){return(this.get("content-type")||"").includes(type)}}}export default createRequestWrapper;
import http from"http";import KempoSocket from"./socket.js";import{validateHandshake}from"./handshake.js";import{CLOSE_CODES}from"./frames.js";import{sockets,closeAll}from"./registry.js";import createRequestWrapper from"../requestWrapper.js";import createResponseWrapper from"../responseWrapper.js";export{sockets,broadcast,closeAll}from"./registry.js";export{default as KempoSocket}from"./socket.js";export{CLOSE_CODES,OPCODES}from"./frames.js";export const defaultWebsocketConfig={enabled:!0,maxMessageSize:1048576,allowedOrigins:null,requireOrigin:!1,heartbeatInterval:3e4,heartbeatTimeout:1e4};const SKIPPED_UPGRADE_HEADERS=new Set(["content-type","content-length","content-encoding","transfer-encoding","connection","upgrade"]),writeHttpResponse=(socket,status,message,extraHeaders={})=>{if(socket.destroyed)return;const body=message||http.STATUS_CODES[status]||"",headers={"Content-Type":"text/plain","Content-Length":Buffer.byteLength(body),Connection:"close",...extraHeaders},head=Object.entries(headers).map(([name,value])=>`${name}: ${value}`).join("\r\n");socket.end(`HTTP/1.1 ${status} ${http.STATUS_CODES[status]||"Error"}\r\n${head}\r\n\r\n${body}`)};export const createUpgradeHandler=({resolveRoute:resolveRoute,loadModule:loadModule,runMiddleware:runMiddleware,config:config,log:log})=>{const websocketConfig={...defaultWebsocketConfig,...config.websocket||{}},hookedServers=new WeakSet;return async(request,socket,head)=>{const server=socket.server;if(server&&!hookedServers.has(server)){hookedServers.add(server);const close=server.close.bind(server);server.close=(...args)=>{for(const open of sockets({filter:candidate=>candidate.server===server}))open.close(CLOSE_CODES.GOING_AWAY,"Server shutting down");return close(...args)}}if(socket.on("error",error=>{log(`WebSocket socket error before upgrade: ${error.message}`,3)}),!websocketConfig.enabled)return writeHttpResponse(socket,404,"Not Found");const handshake=validateHandshake(request,websocketConfig);if(!handshake.ok)return log(`WebSocket handshake rejected (${handshake.status}): ${handshake.message}`,2),writeHttpResponse(socket,handshake.status,handshake.message,handshake.headers);const requestPath=(request.url||"/").split("?")[0];let route;try{route=await resolveRoute(requestPath)}catch(error){return log(`WebSocket route resolution failed for ${requestPath}: ${error.message}`,0),writeHttpResponse(socket,500,"Internal Server Error")}if(!route)return log(`WebSocket 404 - no WS.js for: ${requestPath}`,1),writeHttpResponse(socket,404,"Not Found");const enhancedRequest=createRequestWrapper(request,route.params),rawResponse=new http.ServerResponse(request);rawResponse.assignSocket(socket),rawResponse.shouldKeepAlive=!1,rawResponse.once("finish",()=>socket.end());const enhancedResponse=createResponseWrapper(rawResponse);let reached=!1;try{await runMiddleware(enhancedRequest,enhancedResponse,async()=>{reached=!0})}catch(error){return log(`WebSocket middleware threw for ${requestPath}: ${error.message}`,0),rawResponse.headersSent||(rawResponse.statusCode=500,rawResponse.setHeader("Content-Type","text/plain")),void(rawResponse.writableEnded||rawResponse.end("Internal Server Error"))}if(!reached)return log(`WebSocket upgrade rejected by middleware: ${requestPath}`,2),void(rawResponse.writableEnded||rawResponse.end());const collectedHeaders=rawResponse.getHeaders();let handler;rawResponse.detachSocket(socket);try{handler=await loadModule(route.filePath)}catch(error){return log(`Failed to load WebSocket route ${route.filePath}: ${error.message}`,0),writeHttpResponse(socket,500,"Internal Server Error")}if("function"!=typeof handler)return log(`WebSocket route does not export a function: ${route.filePath}`,0),writeHttpResponse(socket,500,"Route file does not export a function");const kempoSocket=new KempoSocket({socket:socket,server:server,path:requestPath,params:route.params,query:enhancedRequest.query,headers:request.headers,cookies:enhancedRequest.cookies,config:websocketConfig,log:log});try{await handler(enhancedRequest,kempoSocket)}catch(error){return log(`WebSocket route threw for ${requestPath}: ${error.message}`,0),writeHttpResponse(socket,500,"Internal Server Error")}const rejection=kempoSocket.rejection;if(rejection)return log(`WebSocket upgrade refused by route (${rejection.status}): ${requestPath}`,2),writeHttpResponse(socket,rejection.status,rejection.message);socket.destroyed?log(`WebSocket client disconnected before the handshake completed: ${requestPath}`,3):(((socket,accept,extraHeaders)=>{const lines=["HTTP/1.1 101 Switching Protocols","Upgrade: websocket","Connection: Upgrade",`Sec-WebSocket-Accept: ${accept}`];for(const[name,value]of Object.entries(extraHeaders))SKIPPED_UPGRADE_HEADERS.has(name.toLowerCase())||lines.push(`${name}: ${Array.isArray(value)?value.join(", "):value}`);socket.write(`${lines.join("\r\n")}\r\n\r\n`)})(socket,handshake.accept,collectedHeaders),kempoSocket.accept(head),log(`WebSocket connected: ${requestPath}`,2))}};export const closeAllSockets=(reason="Server shutting down")=>{const open=sockets();return closeAll(CLOSE_CODES.GOING_AWAY,reason),open.length};export default createUpgradeHandler;